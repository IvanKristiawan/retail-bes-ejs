<style>
    a {
        color: inherit; /* blue colors for links too */
        text-decoration: inherit; /* no underline */
    }
</style>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th scope="col">Kode</th>
            <th scope="col">Nama Stok</th>
            <th scope="col">Qty</th>
            <th scope="col">Harga</th>
            <th scope="col">Potongan</th>
            <th scope="col">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        <% for(let ApenjualanStok of ApenjualanStoks) { %> 
                <tr>
                    <th scope="row"><%= ApenjualanStok.kodeStok %></th>  
                    <% for(let stok of stoks) {%> 
                        <% if(stok.kode == ApenjualanStok.kodeStok) {%>
                            <td><a href="/penjualanStok/<%= penjualanStok.nomorNota %>/<%= stok.kode %>"><%= stok.namaStok %></a></td>
                        <% } %>     
                    <% } %>     
                    <td><%= ApenjualanStok.qty %></td>
                    <td><%= ApenjualanStok.hargaSatuan %></td>
                    <td><%= ApenjualanStok.potongan %>%</td>
                    <td><%= ApenjualanStok.subtotal %></td>
                </tr>
        <% } %> 
    </tbody>
</table>